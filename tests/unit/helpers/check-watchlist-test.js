import { checkWatchlist } from '../../../helpers/check-watchlist';
import { module, test } from 'qunit';

module('Unit | Helper | check watchlist');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkWatchlist(42);
  assert.ok(result);
});
